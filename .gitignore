# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── IDEs / SO ────────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Configuración local de Claude Code (personal, no se versiona) ────────
.claude/settings.local.json

# ── Resultados de validación Sim2Real ────────────────────────────────────
# Se regeneran en cada corrida con Unity; contienen datos de la última
# ejecución local y no deben versionarse.
TMR2026/validation_results/
TMR2026/validation_results_parking/

# ── Logs y artefactos de entrenamiento ───────────────────────────────────
TMR2026/logs/
runs/
runs_train_signs.log
# Copia local del data.yaml con ruta ABSOLUTA (solo válida en esta PC).
traffic_lights/data_local.yaml

# ── DriveNet datasets + trained artifacts (regenerable, can be large) ─────
# Tubs (synthetic/recorded) and demo weights are produced by the DriveNet
# tools; only the code is versioned. See TMR2026/docs/DRIVE_NET.md.
TMR2026/datasets/
TMR2026/weights/drive_net.pt
TMR2026/weights/drive_net.json
TMR2026/weights/drive_net.torchscript
TMR2026/weights/drive_net.onnx
TMR2026/weights/drive_net.ncnn.*
TMR2026/weights/drive_eval.png
TMR2026/weights/drive_train.png
