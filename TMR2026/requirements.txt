# ============================================================
# Dependencias del sistema TMR 2026
# Instalar en Raspberry Pi OS Bookworm (Debian 12)
# ============================================================

# --- Paquetes APT (ejecutar PRIMERO, antes del pip install) ---
# sudo apt update && sudo apt install -y \
#   python3-picamera2 \
#   python3-libcamera \
#   imx500-all \
#   python3-pygame \
#   python3-rpi.gpio \
#   python3-smbus2 \
#   bluetooth \
#   bluez \
#   python3-dbus

# --- Paquetes pip ---
# (Algunos ya vienen en el sistema, se listan para referencia)

# Adafruit CircuitPython (I2C, PCA9685, VL53L0X, servo)
adafruit-circuitpython-pca9685
adafruit-circuitpython-motor
adafruit-circuitpython-vl53l0x
adafruit-blinka
adafruit-extended-bus      # ExtendedI2C — buses /dev/i2c-3 y /dev/i2c-4

# GPIO Pi 5 (chip 4) — backend principal; RPi.GPIO es el fallback
lgpio

# Visión
opencv-python-headless>=4.8

# Numpy (normalmente preinstalado)
numpy>=1.24

# Detección de señales YOLOv8 (weights/tmr_signs.pt).
# Si no está instalado, SignDetector degrada al detector por color.
ultralytics>=8.0

# --- Solo PC / Sim2Real (validación, NO necesario en el Pi) ---
# matplotlib: gráficas del artículo (analyze_results.py)
matplotlib>=3.7

# --- Desarrollo (tests) ---
pytest>=7.0
